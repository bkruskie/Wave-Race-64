name: Update Badges

on:
  push:
    branches: [master]

jobs:
  update-badges:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install build dependencies
        run: |
          pip install meson ninja mapfile_parser>=2.12.1,<3.0.0 GitPython colour

      - name: Build project (COMPARE=0)
        run: make COMPARE=0

      - name: Generate Progress Report
        run: python3 -m mapfile_parser objdiff_report --version us

      - name: Update badges
        run: python3 tools/update_badges.py

      - name: Commit badge updates
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "actions@github.com"
          git add badges/
          git commit -m "Update badges: ${{ github.sha }}"
          git push